import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const StoryCircleListItem = () => {
  return (
    <View>
      <Text>StoryCircleListItem</Text>
    </View>
  )
}

export default StoryCircleListItem

const styles = StyleSheet.create({})